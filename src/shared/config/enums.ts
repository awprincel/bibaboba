export enum ELSNames {
    ACCESS_TOKEN = "accessToken",
    REFRESH_TOKEN = "refreshToken",
    USER = "user"
}

export enum ERoutePaths {
    HOME = "home",
    REGISTER = "register",
    LOGIN = "login",
    SPACES = "spaces",
    SPACE = "space",
    BOOKINGS = "bookings",
    BOOKING = "booking",
    NOT_FOUND = "notFound",
}